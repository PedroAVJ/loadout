interface:
  display_name: "Oracle"
  short_description: "Ask Claude Fable 5"
  icon_small: "./assets/oracle-icon.svg"
  icon_large: "./assets/oracle-icon.svg"
  brand_color: "#0EA37F"
  default_prompt: "When asked to use Oracle, first gather verified local context and create one focused prompt. Send it through the authenticated Claude Code CLI using the fail-closed helper with --model claude-fable-5, CLAUDE_CODE_NO_MODEL_FALLBACK=1, no effort override, restricted tools, JSON output, and no session persistence. Require modelUsage to prove that claude-fable-5 produced substantive output and that no disallowed model produced output. Do not use the fable alias, do not silently substitute another model, and do not return an Oracle answer if Fable 5 is unavailable. Report Codex's grounded verdict, Fable's analysis, the local evidence check, and the smallest remaining verification steps."
