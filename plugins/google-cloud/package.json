{
  "name": "@loadout/google-cloud",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Google Cloud plugin: gcloud conventions, Cloud Storage substrate, and the publish-file CLI.",
  "bin": {
    "publish-file": "./bin/publish-file"
  },
  "scripts": {
    "check": "node --check bin/publish-file.mjs && node --test ./tests",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@google-cloud/storage": "7.21.0",
    "commander": "^14.0.2"
  }
}
